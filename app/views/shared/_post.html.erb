<ol>
  <li id="post-<%= post.id %>">
    <div class="row content">
      <div class="col-md-4">
        <% if post.picture? %>
          <a href="#" class="thumbnail", title="<%=post.title%>">
            <%= image_tag post.picture.url  %>
          </a>
        <% end %>
      </div>
      <div class="col-md-6">
        <div class="post-text">
          <% if current_user && current_user.admin? %>
            <%= render 'user_profile_link', post: post %>
          <% end %>
        </div>
        <div class="timestamp">
          <p>Posted <%= time_ago_in_words(post.created_at) %> ago.</p>
        </div>
        <div class="post-text">
          <p><%= post.content %></h1>
        </div>
        <div class="post-text">
          <% if current_user && current_user.admin? %>
            <%= render 'post_delete_link' %>
          <% end %>
          <%= link_to "Kommentoi", new_comment_path(post_params(post)), class:"btn btn-link" %>
        </div>
      </div>
    </div>
  </li>

  <%= render partial:'shared/post', collection: post.comments, as: :post %>
</ol>
