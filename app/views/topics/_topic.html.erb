<li id="topic-<%= topic.id %>">
  <div class="row content">
    <div class="col-md-1">
      <% if current_user %>
        <%= render 'topics/vote', post: topic %>
      <% end %>
    </div>
    <div class="col-md-4">
      <% if topic.picture? %>
        <a href="#" class="thumbnail", title="<%=topic.title%>">
          <%= image_tag topic.picture.url  %>
        </a>
      <% end %>
    </div>
      <div class="col-md-5">
        <div class="post-text">
          <%= link_to topic.title, topic_path(topic), :class => "topic_title" %>
        </div>
        <div class="post-text">
          <% if current_user && current_user.admin? %>
            <%= render 'shared/user_profile_link', post: topic %>
          <% end %>
        </div>
        <div class="timestamp">
          <p>Posted <%= time_ago_in_words(topic.created_at) %> ago.</p>
        </div>
        <div class="post-text">
          <% if current_user && current_user.admin? %>
            <%= render 'shared/post_delete_link', post: topic %>
          <% end %>
        </div>
  </div>
</li>
